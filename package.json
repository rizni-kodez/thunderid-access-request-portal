{
  "name": "thunderid-access-request-portal",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "ui-web",
    "api-access-mgmt"
  ],
  "scripts": {
    "dev:ui": "npm run dev --workspace ui-web",
    "dev:api": "npm run dev --workspace api-access-mgmt",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces --if-present"
  }
}
